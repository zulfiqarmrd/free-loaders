[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
paho-mqtt = "~=1.6"
psutil = "~=5.9"
jetson-stats = { version = "~=3.1", markers = "'tegra' in platform_release" }

[dev-packages]

[requires]
python_version = "3"
